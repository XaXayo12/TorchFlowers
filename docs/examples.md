# Examples

Examples are in `bots/bedrock-engine/examples`.

All examples read account, server, API, and token settings from environment variables. They are intended to compile by default and run only when the required environment is configured.
